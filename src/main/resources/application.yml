server:
  port: 8080

spring:
   profiles:
     active: dev
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    # 再次确认这里没有多余的空格
#    url: jdbc:mysql://localhost:3306/campus_help_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
#    username: root
#    password: "" # 直接写在这里，带上引号

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl